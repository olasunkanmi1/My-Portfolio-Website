import styled from "styled-components";

export const SkillsStyled = styled.section`

.wrap {
  display: flex;

  .skill {
    display: flex;
    flex-direction: column;
    align-items: center;

    svg {
      width: 100px;
      height: 100px;
    }
  }
}
`;
